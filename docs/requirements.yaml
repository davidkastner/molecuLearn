name: docs
channels:
dependencies:
    # Base depends
  - python
  - pip

    # Pip-only installs
  - pip:
    - sphinx==6.0.0
    - sphinx-autoapi
    - biopandas
    - https://github.com/revitron/revitron-sphinx-theme/archive/master.zip
