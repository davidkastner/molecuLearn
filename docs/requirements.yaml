name: docs
channels:
dependencies:
    # Base depends
  - python
  - pip

    # Pip-only installs
  - pip:
    - numpy
    - matplotlib
    - shap
    - lime
    - sphinx==6.0.0
    - sphinx-autoapi
    - https://github.com/revitron/revitron-sphinx-theme/archive/master.zip
