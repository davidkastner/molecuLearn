name: docs
channels:
dependencies:
    # Base depends
  - python
  - pip

    # Pip-only installs
  - pip:
    - biopandas
    - lime
    - matplotlib
    - MDAnalysis
    - numpy
    - pandas
    - torch
    - scikit-learn
    - seaborn
    - shap
    - sphinx_rtd_theme==1.1.1
    - sphinx-autoapi
    - https://github.com/revitron/revitron-sphinx-theme/archive/refs/tags/0.7.0.zip
