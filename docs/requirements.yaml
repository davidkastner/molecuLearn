name: docs
channels:
dependencies:
    # Base depends
  - python
  - pip

    # Package depends
  - matplotlib
  - numpy
  - pandas
  - pandas
  - pytorch
  - seaborn
  - scikit-learn

    # Pip-only installs
  - pip:
    - sphinx-autoapi
    - biopandas
    - https://github.com/revitron/revitron-sphinx-theme/archive/master.zip
