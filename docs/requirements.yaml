name: docs
channels:
dependencies:
    # Base depends
  - python
  - pip

    # Pip-only installs
  - pip:
    - biopandas
    - lime
    - matplotlib
    - MDAnalysis
    - numpy
    - pandas
    - torch
    - scikit-learn
    - seaborn
    - shap
    - sphinx==6.0.0
    - sphinx-autoapi
    - https://github.com/revitron/revitron-sphinx-theme/archive/master.zip
